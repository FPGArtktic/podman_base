# ---------------------------------------------------------------------------
# Base package set installed from the official Arch repositories (core/extra).
# One package name per line. Blank lines and #-comments are ignored.
#
# Do NOT edit this file to add your own packages -> use extra-pacman.txt.
# ---------------------------------------------------------------------------

# --- toolchain / build ------------------------------------------------------
base-devel
gcc
make
cmake
pkgconf

# --- shells -----------------------------------------------------------------
zsh
zsh-completions
bash-completion

# --- editors ----------------------------------------------------------------
neovim
vim
nano

# --- requested tooling ------------------------------------------------------
mc
superfile
lazygit
git
tmux
btop
lynx
fzf
rsync

# --- everyday CLI -----------------------------------------------------------
sudo
openssh
curl
wget
less
man-db
man-pages
which
tree
jq
ripgrep
fd
bat
eza
git-delta
shellcheck
diffutils
procps-ng
# fastfetch: maintained companion / fallback for neofetch (AUR, see aur.txt)
fastfetch
psmisc
inetutils
ca-certificates
gnupg

# --- archives ---------------------------------------------------------------
tar
gzip
zip
unzip
xz
zstd

# --- language runtimes (LSP servers + Neovim providers need these) ----------
python
python-pip
python-pynvim
nodejs
npm
go
tree-sitter
tree-sitter-cli

# --- clipboard bridges (Neovim clipboard=unnamedplus) -----------------------
wl-clipboard
xclip
